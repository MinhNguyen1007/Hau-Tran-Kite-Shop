# deps
node_modules/

# next
.next/
out/
build/
dist/

# env — KHÔNG BAO GIỜ commit
.env
.env.*
!.env.example

# secrets
*.pem
*.key
# File Google Cloud Console cho tải về lúc tạo OAuth client — chứa client_secret dạng chữ thường.
# Tên nó không khớp mẫu .env* nào nên `git add .` sẽ nuốt gọn nếu không chặn ở đây.
client_secret_*.json

# supabase local
supabase/.temp/
supabase/.branches/

# test / tooling
coverage/
.eslintcache
*.tsbuildinfo

# next auto-generated
next-env.d.ts

# claude code — permission cá nhân từng máy, không share
.claude/settings.local.json

# deploy
.vercel

# os
.DS_Store
Thumbs.db
